import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Weather_Openweathermap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
